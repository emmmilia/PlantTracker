<Window x:Class="PlantTracker.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PlantTracker.Wpf"
        mc:Ignorable="d"
        Loaded="MainWindow_Loaded"
        Title="MainWindow"
        Height="450"
        Width="800">
    <Grid>
        <Button Content="Add Plant" Click="AddPlantButton_Click" 
                HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10"/>
        <ListView ItemsSource="{Binding Plants}" Margin="10,50,10,10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Water" Click="WaterButton_Click" CommandParameter="{Binding Id}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Name"
                                    DisplayMemberBinding="{Binding Name}"/>
                    <GridViewColumn Header="Species"
                                    DisplayMemberBinding="{Binding Species.Name}"/>
                    <GridViewColumn Header="Status"
                                    DisplayMemberBinding="{Binding PlantStatus}"/>
                    <GridViewColumn Header="Last Watered"
                                    DisplayMemberBinding="{Binding LastWatered}"/>
                    <GridViewColumn Header="Notes"
                                    DisplayMemberBinding="{Binding Notes}"/>
                    <GridViewColumn Header="">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Edit" Click="EditButton_Click" CommandParameter="{Binding Id}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Delete" Click="DeleteButton_Click" CommandParameter="{Binding Id}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
